query Starships($first: Int, $after: String) {
    allStarships(first: $first, after: $after) {
        starships {
            id
            name
            filmConnection {
                totalCount
            }
        }
        pageInfo {
            hasNextPage
            endCursor
        }
    }
}
