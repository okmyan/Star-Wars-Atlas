query Planets($first: Int, $after: String) {
    allPlanets(first: $first, after: $after) {
        planets {
            id
            name
            filmConnection {
                totalCount
            }
        }
        pageInfo {
            hasNextPage
            endCursor
        }
    }
}
